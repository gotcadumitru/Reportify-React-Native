import {put, takeEvery} from 'redux-saga/effects';
// * Action types

// * Generators

function* firstGenerator() {
  try {
  } catch (error) {}
}

// * Watcher
export function* appActionWatcher() {
  // yield takeEvery('*', firstGenerator);
}
